
(function (a, i, g) {
    a.fn.tinyNav = function (j) {
		var count=0;
        var b = a.extend({ active: "selected", header: "", label: "" }, j); return this.each(function () {
			
            g++; 
			var h = a(this), d = "tinynav" + g, f = ".l_" + d, e = a("<select/>").attr("id", d).addClass("tinynav " + d); if (h.is("ul,ol")) {
                "" !== b.header && e.append(a("<option/>").text(b.header)); var c = ""; h.addClass("l_" + d).find("li").each(function () { 
				//alert(count);
				count+=1;
				var name=count;
				c += '<option value="' + name + '" data-slide="'+count+'">'; var b; for (b = 0; b < a(this).parents("ul, ol").length - 1; b++) c += "- "; c += a(this).text() + "</option>" }); e.append(c);	
                b.header || e.find(":eq(" + a(f + " li a").index(a(f + " li a." + b.active)) + ")").attr("selected", !0); e.change(function () { 
				if(a(this).val()!="10")
				{
		
        htmlbody.animate({
            scrollTop: $('.slide[data-slide="' + a(this).val() + '"]').offset().top
        }, 2000, 'easeInOutQuint');
   
				}
				else
				{
					window.open('blog.html', '_blank');
				}
				//i.location.href = a(this).val() 
				}); a(f).after(e); b.label && e.before(a("<label/>").attr("for", d).addClass("tinynav_label " + d + "_label").append(b.label))
            } 
        })
    } 
})(jQuery, this, 0);

$(function () {
    $('#nav11').tinyNav({
     active: 'active'
    });
});
